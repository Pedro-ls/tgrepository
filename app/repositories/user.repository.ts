import Repo from "repository/lib/repository";

export class UserRepository extends Reposito {
  constructor() {
    super(User);
    repos;
  }
  public static todos() {
    console.log("teste");
  }
}
